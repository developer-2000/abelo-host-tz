{
  "name": "abelo-host-tz",
  "version": "1.0.0",
  "description": "Cursor Blog - PHP + Smarty blog",
  "scripts": {
    "dev": "sass --watch assets/scss/main.scss:public/assets/css/main.css --no-source-map --style expanded --poll",
    "build": "sass assets/scss/main.scss:public/assets/css/main.css --style compressed --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.70.0"
  }
}
